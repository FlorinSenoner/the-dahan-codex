---
phase: 02-spirit-library
plan: 05
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - app/routes/spirits.$slug.tsx
  - app/router.tsx
  - app/styles/globals.css
autonomous: true

must_haves:
  truths:
    - "Navigating to /spirits/{slug} shows the spirit detail page"
    - "Spirit image and name animate during transition from list"
    - "Aspect parameter loads the correct aspect variant"
    - "Back button returns to spirit list"
  artifacts:
    - path: "app/routes/spirits.$slug.tsx"
      provides: "Spirit detail route with Convex query"
      contains: "createFileRoute"
    - path: "app/router.tsx"
      provides: "Router with view transitions enabled"
      contains: "defaultViewTransition"
  key_links:
    - from: "app/routes/spirits.$slug.tsx"
      to: "convex/spirits.ts"
      via: "useQuery(api.spirits.getSpiritBySlug)"
      pattern: "useQuery.*getSpiritBySlug"
    - from: "app/router.tsx"
      to: "app/routes/spirits.tsx"
      via: "view transition configuration"
      pattern: "defaultViewTransition.*true"
---

<objective>
Create the spirit detail route and enable View Transitions API for smooth navigation animations between list and detail.

Purpose: Complete the navigation flow - users can tap spirits and see detail with animated transition.
Output: Working /spirits/{slug} page with view transitions from list.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-spirit-library/02-CONTEXT.md
@.planning/phases/02-spirit-library/02-RESEARCH.md
@app/router.tsx
@convex/spirits.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spirit detail route</name>
  <files>app/routes/spirits.$slug.tsx</files>
  <action>
Create `app/routes/spirits.$slug.tsx`:

```typescript
import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { useQuery } from "convex/react";
import { api } from "convex/_generated/api";
import { z } from "zod";
import { ArrowLeft } from "lucide-react";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { cn } from "@/lib/utils";

// Search params for aspect selection
const detailSearchSchema = z.object({
  aspect: z.string().optional(),
});

export const Route = createFileRoute("/spirits/$slug")({
  validateSearch: detailSearchSchema,
  component: SpiritDetailPage,
});

// Complexity badge colors (same as spirit-row)
const complexityColors: Record<string, string> = {
  Low: "bg-element-plant/20 text-element-plant border-element-plant/30",
  Moderate: "bg-element-sun/20 text-element-sun border-element-sun/30",
  High: "bg-element-fire/20 text-element-fire border-element-fire/30",
  "Very High": "bg-destructive/20 text-destructive border-destructive/30",
};

// Element badge colors
const elementColors: Record<string, string> = {
  Sun: "bg-element-sun/20 text-element-sun border-element-sun/30",
  Moon: "bg-element-moon/20 text-element-moon border-element-moon/30",
  Fire: "bg-element-fire/20 text-element-fire border-element-fire/30",
  Air: "bg-element-air/20 text-element-air border-element-air/30",
  Water: "bg-element-water/20 text-element-water border-element-water/30",
  Earth: "bg-element-earth/20 text-element-earth border-element-earth/30",
  Plant: "bg-element-plant/20 text-element-plant border-element-plant/30",
  Animal: "bg-element-animal/20 text-element-animal border-element-animal/30",
};

function SpiritDetailPage() {
  const { slug } = Route.useParams();
  const { aspect } = Route.useSearch();
  const navigate = useNavigate();

  const spirit = useQuery(api.spirits.getSpiritBySlug, {
    slug,
    aspect,
  });

  // Loading state
  if (spirit === undefined) {
    return (
      <div className="min-h-screen bg-background">
        <header className="sticky top-0 z-10 bg-background/95 backdrop-blur border-b border-border px-4 py-3">
          <Button
            variant="ghost"
            size="icon"
            onClick={() => navigate({ to: "/spirits" })}
          >
            <ArrowLeft className="h-5 w-5" />
          </Button>
        </header>
        <div className="p-4 animate-pulse">
          <div className="w-full h-[200px] bg-muted rounded-lg mb-4" />
          <div className="h-8 bg-muted rounded w-3/4 mb-2" />
          <div className="h-4 bg-muted rounded w-full mb-4" />
          <div className="flex gap-2">
            <div className="h-6 bg-muted rounded w-16" />
            <div className="h-6 bg-muted rounded w-16" />
          </div>
        </div>
      </div>
    );
  }

  // Not found state
  if (spirit === null) {
    return (
      <div className="min-h-screen bg-background">
        <header className="sticky top-0 z-10 bg-background/95 backdrop-blur border-b border-border px-4 py-3">
          <Button
            variant="ghost"
            size="icon"
            onClick={() => navigate({ to: "/spirits" })}
          >
            <ArrowLeft className="h-5 w-5" />
          </Button>
        </header>
        <div className="flex flex-col items-center justify-center p-8 text-center min-h-[60vh]">
          <p className="text-xl font-serif text-foreground mb-2">
            Spirit Not Found
          </p>
          <p className="text-muted-foreground mb-4">
            The spirit "{slug}" doesn't exist.
          </p>
          <Link to="/spirits" className="text-primary hover:underline">
            Back to Spirits
          </Link>
        </div>
      </div>
    );
  }

  // Determine if showing an aspect
  const isAspect = !!spirit.aspectName;
  const displayName = isAspect
    ? `${spirit.name} (${spirit.aspectName})`
    : spirit.name;

  return (
    <div className="min-h-screen bg-background pb-20">
      {/* Header with back button */}
      <header className="sticky top-0 z-10 bg-background/95 backdrop-blur border-b border-border px-4 py-3">
        <div className="flex items-center gap-3">
          <Button
            variant="ghost"
            size="icon"
            onClick={() => navigate({ to: "/spirits" })}
          >
            <ArrowLeft className="h-5 w-5" />
          </Button>
          <span className="font-serif text-lg font-medium truncate">
            {spirit.name}
          </span>
        </div>
      </header>

      {/* Spirit content */}
      <main className="p-4">
        {/* Hero image */}
        <div className="relative w-full aspect-square max-w-[300px] mx-auto mb-6">
          <img
            src={spirit.imageUrl}
            alt={displayName}
            className="w-full h-full object-cover rounded-xl"
            style={{
              viewTransitionName: isAspect
                ? `spirit-aspect-${spirit.aspectName?.toLowerCase()}`
                : `spirit-image-${slug}`,
            }}
          />
        </div>

        {/* Spirit name */}
        <h1
          className="font-serif text-2xl font-bold text-foreground text-center mb-2"
          style={{
            viewTransitionName: isAspect ? undefined : `spirit-name-${slug}`,
          }}
        >
          {displayName}
        </h1>

        {/* Aspect indicator */}
        {isAspect && (
          <p className="text-center text-muted-foreground text-sm mb-4">
            Aspect of {spirit.name.replace(` (${spirit.aspectName})`, "")}
          </p>
        )}

        {/* Complexity badge */}
        <div className="flex justify-center mb-4">
          <Badge
            variant="outline"
            className={cn("text-sm", complexityColors[spirit.complexity] || "")}
          >
            {spirit.complexity} Complexity
          </Badge>
        </div>

        {/* Summary */}
        <p className="text-muted-foreground text-center max-w-md mx-auto mb-6">
          {spirit.summary}
        </p>

        {/* Elements */}
        <div className="flex flex-wrap justify-center gap-2 mb-8">
          {spirit.elements.map((element) => (
            <Badge
              key={element}
              variant="outline"
              className={cn("text-xs", elementColors[element] || "")}
            >
              {element}
            </Badge>
          ))}
        </div>

        {/* Placeholder for future content (Phase 3) */}
        <div className="border border-border rounded-lg p-4 bg-card">
          <p className="text-sm text-muted-foreground text-center">
            Overview, Growth, and Presence tracks coming in Phase 3
          </p>
        </div>
      </main>
    </div>
  );
}
```

**Note:** The detail page is intentionally minimal for Phase 2. Phase 3 will add Overview tab, radar chart, growth options, and presence tracks.
  </action>
  <verify>
Run `pnpm typecheck` - should pass.
Visit /spirits/river-surges-in-sunlight - should show River's detail page.
Visit /spirits/river-surges-in-sunlight?aspect=sunshine - should show Sunshine aspect.
Back button should return to /spirits.
  </verify>
  <done>
- Spirit detail route exists at /spirits/$slug
- Aspect query param loads aspect variant
- Loading and not-found states handled
- Spirit info displayed (image, name, complexity, summary, elements)
- Back button navigation works
  </done>
</task>

<task type="auto">
  <name>Task 2: Enable View Transitions in router and add CSS</name>
  <files>
    app/router.tsx
    app/styles/globals.css
  </files>
  <action>
**Step 1: Update router.tsx to enable View Transitions**

Update `app/router.tsx`:

```typescript
import { createRouter as createTanStackRouter } from "@tanstack/react-router";
import { routeTree } from "./routeTree.gen";

function NotFound() {
  return (
    <div style={{ padding: "2rem", textAlign: "center" }}>
      <h1>404 - Page Not Found</h1>
      <p>The page you're looking for doesn't exist.</p>
      <a href="/">Go back home</a>
    </div>
  );
}

export function createRouter() {
  const router = createTanStackRouter({
    routeTree,
    scrollRestoration: true,
    defaultNotFoundComponent: NotFound,
    // Enable View Transitions API globally
    defaultViewTransition: true,
  });

  return router;
}

export function getRouter() {
  return createRouter();
}

declare module "@tanstack/react-router" {
  interface Register {
    router: ReturnType<typeof createRouter>;
  }
}
```

**Step 2: Add View Transition CSS to globals.css**

Add the following CSS to `app/styles/globals.css` (after the theme variables):

```css
/* View Transitions */
::view-transition-group(root) {
  animation-duration: 400ms;
  animation-timing-function: ease-in-out;
}

/* Spirit image transitions */
::view-transition-old(spirit-image-*),
::view-transition-new(spirit-image-*) {
  animation-duration: 500ms;
  animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
}

/* Spirit name transitions */
::view-transition-old(spirit-name-*),
::view-transition-new(spirit-name-*) {
  animation-duration: 400ms;
  animation-timing-function: ease-out;
}

/* Aspect image transitions */
::view-transition-old(spirit-aspect-*),
::view-transition-new(spirit-aspect-*) {
  animation-duration: 500ms;
  animation-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
}

/* Respect reduced motion preference */
@media (prefers-reduced-motion: reduce) {
  ::view-transition-group(*),
  ::view-transition-old(*),
  ::view-transition-new(*) {
    animation-duration: 0.01ms !important;
  }
}
```

**Note:** The `spirit-image-*` and `spirit-name-*` wildcards work with CSS View Transitions because each element has a unique `view-transition-name` (e.g., `spirit-image-river-surges-in-sunlight`). The CSS selector uses `*` as a wildcard to match all spirits.
  </action>
  <verify>
Run `pnpm typecheck` - should pass.
Visit /spirits, tap a spirit - image should animate smoothly to detail page.
Navigate back - animation should reverse.
Check with Chrome DevTools > Rendering > "Emulate CSS media feature prefers-reduced-motion" - animations should be instant.
  </verify>
  <done>
- Router has defaultViewTransition: true
- View transition CSS defines animation timing
- Spirit image and name animate between list and detail
- prefers-reduced-motion is respected
  </done>
</task>

</tasks>

<verification>
After completing both tasks:
1. `pnpm typecheck` passes
2. /spirits/{slug} route loads spirit detail
3. Aspect query param loads correct variant
4. View transitions animate smoothly between list and detail
5. Back button works
6. prefers-reduced-motion preference is respected
</verification>

<success_criteria>
- Spirit detail page shows all spirit info
- View Transitions create smooth navigation experience
- Aspect variants load correctly via query param
- Animations respect user motion preferences
</success_criteria>

<output>
After completion, create `.planning/phases/02-spirit-library/02-05-SUMMARY.md`
</output>
