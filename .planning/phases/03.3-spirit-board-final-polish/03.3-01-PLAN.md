---
phase: 03.3-spirit-board-final-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/spirits/growth-panel.tsx
autonomous: true

must_haves:
  truths:
    - "G1/G2/G3 labels are hidden by default"
    - "G1/G2/G3 labels appear on hover"
    - "Labels are positioned absolute top-right of growth cards"
    - "Touch devices still see functional growth options (labels are supplementary)"
  artifacts:
    - path: "app/components/spirits/growth-panel.tsx"
      provides: "Hover-reveal G1/G2/G3 labels"
      contains: "group-hover"
  key_links:
    - from: "growth-panel.tsx"
      to: "Tailwind group-hover"
      via: "CSS utility classes"
      pattern: "group.*group-hover"
---

<objective>
Update growth panel labels to show on hover only using Tailwind group-hover pattern.

Purpose: Make growth cards more compact by hiding G1/G2/G3 labels until user hovers, while keeping labels accessible for reference.
Output: Updated GrowthOptionCard with hover-reveal labels positioned absolute top-right.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-spirit-board-final-polish/03.3-RESEARCH.md

@app/components/spirits/growth-panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update GrowthOptionCard with hover-reveal labels</name>
  <files>app/components/spirits/growth-panel.tsx</files>
  <action>
Modify the GrowthOptionCard component to use Tailwind's group-hover pattern for G1/G2/G3 labels:

1. Add `group` class to the card wrapper div (the one with `bg-muted/30 rounded-lg p-3 relative`)
2. Move the G1/G2/G3 label span from inline in the actions row to absolute positioned top-right
3. Update label classes to:
   - Position: `absolute top-1 right-1 z-10`
   - Hidden by default: `hidden group-hover:inline-block`
   - Keep existing styling: `text-xs font-semibold text-muted-foreground bg-muted/50 backdrop-blur-sm px-2 py-0.5 rounded`
4. Remove the label from the flex row (no longer inline with actions)

The existing cost badge (if present) uses `-top-2 -right-2` positioning, so the G1/G2/G3 label at `top-1 right-1` will not conflict.

Pattern from research:
```tsx
<div className="bg-muted/30 rounded-lg p-3 relative group hover:bg-muted/50 transition-colors">
  {/* G1/G2/G3 label - hidden until hover */}
  <span className="absolute top-1 right-1 z-10
                   hidden group-hover:inline-block
                   text-xs font-semibold text-muted-foreground
                   bg-muted/50 backdrop-blur-sm px-2 py-0.5 rounded">
    {option.id}
  </span>

  {/* Cost badge unchanged */}
  {/* Actions row without inline label */}
</div>
```
  </action>
  <verify>
1. `pnpm typecheck` passes
2. `pnpm build` succeeds
3. Browser test: Visit /spirits/river-surges-in-sunlight, confirm G1/G2/G3 labels are hidden until hovering over growth cards
  </verify>
  <done>G1/G2/G3 labels appear only on hover, positioned absolute top-right of each growth card</done>
</task>

</tasks>

<verification>
1. Run `pnpm typecheck` - no type errors
2. Run `pnpm build` - build succeeds
3. Browser verification:
   - Navigate to /spirits/river-surges-in-sunlight
   - Confirm growth cards show icons and modifiers but no G1/G2/G3 labels by default
   - Hover over a growth card - G1/G2/G3 label appears in top-right corner
   - Labels have frosty backdrop-blur effect
</verification>

<success_criteria>
- GrowthOptionCard uses Tailwind group-hover pattern
- G1/G2/G3 labels hidden by default, visible on hover
- Labels positioned absolute top-right with z-10
- No visual conflicts with cost badges
- Growth cards remain functional on touch devices (actions visible, labels supplementary)
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-spirit-board-final-polish/03.3-01-SUMMARY.md`
</output>
