# Plan 06.2-04: ScoreBreakdown Component Extraction

## Goal

Extract shared ScoreBreakdown component used in game-form.tsx and $id.tsx.

## Tasks

1. Create app/components/games/score-breakdown.tsx with shared component
2. Update game-form.tsx to import from shared location
3. Update $id.tsx to import from shared location

## Implementation

```tsx
// app/components/games/score-breakdown.tsx

interface ScoreBreakdownProps {
  result: "win" | "loss";
  difficulty: number;
  playerCount: number;
  cardsRemaining?: number;
  dahanCount?: number;
  blightCount?: number;
}

export function ScoreBreakdown({ ... }: ScoreBreakdownProps) {
  // Shared implementation from existing components
}
```

## Verification

```bash
pnpm typecheck
pnpm jscpd app/components/games/
```

## Files Changed

- app/components/games/score-breakdown.tsx (new)
- app/components/games/game-form.tsx
- app/routes/_authenticated/games/$id.tsx
