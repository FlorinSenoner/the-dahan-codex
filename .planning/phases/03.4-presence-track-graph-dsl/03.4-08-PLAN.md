---
phase: 03.4-presence-track-graph-dsl
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - convex/seed.ts
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "Finder presence tracks match actual spirit panel"
    - "User can see correct energy and card plays values"
    - "Cross-track connection renders correctly"
  artifacts:
    - path: "convex/seed.ts"
      provides: "Corrected Finder of Paths Unseen presence track data"
      contains: "finder-of-paths-unseen"
  key_links:
    - from: "convex/seed.ts"
      to: "Convex database"
      via: "reseedSpirits mutation"
      pattern: "finder-of-paths-unseen"
---

<objective>
Correct Finder of Paths Unseen presence track data from actual spirit panel image.

Purpose: Current data is speculative. User will provide the actual Finder spirit panel image for transcription.

Output: Accurate seed data for Finder's presence tracks
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-presence-track-graph-dsl/03.4-01-SUMMARY.md
@convex/seed.ts (search: finder-of-paths-unseen)
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Provide Finder spirit panel image</action>
  <instructions>
Please provide an image of the Finder of Paths Unseen spirit panel from the physical game or a high-quality scan. The image should clearly show:

1. **Energy Track** (top row) - all slot values from left to right
2. **Card Plays Track** (bottom row) - all slot values from left to right
3. **Any elements** on slots (e.g., Moon, Air)
4. **Reclaim markers** (if any)
5. **Connection point** between tracks (where they branch/connect)
6. **Starting presence markers** (which slots start with presence)

Once you provide the image, I will transcribe the exact values.
  </instructions>
  <resume-signal>Share image or paste transcription if you have it</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Update Finder seed data from panel transcription</name>
  <files>convex/seed.ts</files>
  <action>
After receiving the panel image/transcription, update Finder's presenceTracks in BOTH locations in seed.ts:
1. `seedSpirits` function (around line 1052)
2. `reseedSpirits` function (around line 2532)

The Node-Edge Graph format is already correct. Only update the VALUES:
- Correct energy track values (row 0)
- Correct card plays track values (row 1)
- Add elements arrays where indicated on panel
- Update reclaim markers if needed
- Verify edge connection point (e3 -> c3 or different)

Current speculation (likely wrong):
```typescript
// Energy track (row 0)
{ id: "e0", row: 0, col: 0, value: 0, trackType: "energy" },
{ id: "e1", row: 0, col: 1, value: 1, trackType: "energy" },
...
// Card plays track (row 1)
{ id: "c0", row: 1, col: 0, value: 1, trackType: "cardPlays" },
...
```

Update with actual values from transcription.
  </action>
  <verify>
`pnpm typecheck` passes
Both seedSpirits and reseedSpirits have matching Finder data
  </verify>
  <done>
Finder's presence track data matches actual spirit panel.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reseed database and verify</name>
  <files></files>
  <action>
Run the reseed mutation to update the database:

```bash
npx convex run seed:reseedSpirits
```

Start dev server and verify Finder renders correctly:

```bash
pnpm dev
```

Navigate to http://localhost:3000/spirits/finder-of-paths-unseen

Verify:
- Energy track shows correct values
- Card plays track shows correct values
- Cross-track connection line appears
- Tooltips show accurate information
  </action>
  <verify>
reseedSpirits completes without errors
Finder page loads without console errors
Presence tracks match panel image
  </verify>
  <done>
Finder renders correctly with accurate data.
  </done>
</task>

</tasks>

<verification>
1. Finder presenceTracks matches spirit panel image
2. reseedSpirits completes successfully
3. Finder detail page renders correct presence track layout
4. Tooltips show accurate energy/cardPlays values
</verification>

<success_criteria>
- Finder seed data updated from actual panel (not speculation)
- Both seedSpirits and reseedSpirits have matching Finder data
- Database reseeded successfully
- Visual verification confirms accuracy
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-presence-track-graph-dsl/03.4-08-SUMMARY.md`
</output>
